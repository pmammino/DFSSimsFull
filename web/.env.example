# URL of the worker FastAPI service (server-side only; never exposed to the browser).
# Local dev with the worker on your machine or via docker-compose:
WORKER_API_URL=http://localhost:8000

# Optional shared secret sent as x-api-key to the worker (set on both sides in prod).
WORKER_API_KEY=
