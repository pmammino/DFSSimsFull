__pycache__/
*.pyc
data/
out/.build_stamp.json
.streamlit/secrets.toml

# per-stat sims exceed GitHub 100MB at full slates; regenerable via run_slate.py
deliverables/hitter_stat_sims.npy

# projected-ownership artifact + its pool-change gate: regenerated by
# scripts/build_ownership.py (scheduled refresh), synced via the object store.
deliverables/projected_ownership.csv
deliverables/.ownership_pool_sig.json
